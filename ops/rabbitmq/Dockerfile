# Thin wrapper so Render's "docker" runtime (well-documented, unambiguous
# blueprint syntax) can deploy plain RabbitMQ as a private service, instead
# of relying on Render's less-documented "runtime: image" (prebuilt
# registry image) blueprint key.
FROM rabbitmq:3.13-management-alpine
